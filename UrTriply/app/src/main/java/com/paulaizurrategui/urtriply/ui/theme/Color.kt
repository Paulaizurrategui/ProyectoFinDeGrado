package com.paulaizurrategui.urtriply.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF) // Color base (claro) para el esquema "dark" por defecto del template
val PurpleGrey80 = Color(0xFFCCC2DC) // Variante grisáceo (claro) para "dark"
val Pink80 = Color(0xFFEFB8C8) // Acento rosado (claro) para "dark"

val Purple40 = Color(0xFF6650a4) // Color base (oscuro) para el esquema "light" por defecto del template
val PurpleGrey40 = Color(0xFF625b71) // Variante grisáceo (oscuro) para "light"
val Pink40 = Color(0xFF7D5260) // Acento rosado (oscuro) para "light"